# Quick Log — Séance libre avec wizard

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permettre à un utilisateur de logger une activité sportive (course, football, full body…) en moins de 60 secondes, sans créer de programme au préalable.

**Architecture:** Nouveau wizard multi-étapes `QuickLogScreen` qui collecte le type d'activité et les métadonnées pertinentes, puis crée une Session marquée `endedAt = now` (séance passée). La Session reçoit 5 nouveaux champs optionnels dans `types.ts`. Aucun changement de schéma IDB (les champs s'ajoutent sans index). Le Dashboard expose un bouton d'entrée visible même sans programme actif.

**Tech Stack:** React 18 + TypeScript strict, `noUncheckedIndexedAccess`, IDB via `repo.ts`, Vitest pour les tests.

## Global Constraints

- `strict` + `noUncheckedIndexedAccess` + `noUnusedLocals` + `noUnusedParameters` activés — tout accès d'index `arr[i]` retourne `T | undefined`.
- Tokens CSS uniquement (`var(--accent)`, `var(--surface)`, etc.) — aucune valeur couleur en dur.
- Classes de texte : `.t-display`, `.t-title`, `.t-body`, `.t-caption`, `.t-eyebrow`, `.t-num`.
- Navigation : `typeof params?.x === 'string' ? params.x : undefined` — jamais `params?.x as string`.
- `DB_VERSION` dans `src/db/schema.ts` = 4 — **ne pas bumper** (aucun nouvel index ni store).
- Commits en anglais. Messages : `feat: …`, `test: …`, `chore: …`.

---

### Task 1 — Extension du modèle `Session` + fonction pure `buildActivityParams`

**Files:**
- Modify: `src/types.ts` (interface `Session`)
- Create: `src/utils/quickLog.ts`
- Create: `tests/quickLog.test.ts`

**Interfaces:**
- Produit: `ActivityKind`, `ActivityParams`, `buildActivityParams()` utilisés par Task 2 et 3.

---

- [ ] **Step 1 : Ajouter les nouveaux champs à `Session` dans `src/types.ts`**

Dans `src/types.ts`, après le champ `notes?: string` de `Session`, ajouter :

```ts
/**
 * Distingue les séances programme des séances enregistrées via le wizard
 * Quick Log. Absent = séance programme ou ancienne séance libre.
 */
sessionKind?: 'cardio' | 'team_sport' | 'strength_free' | 'other'
/** Activité spécifique : 'running' | 'cycling' | 'swimming' | 'rowing' | 'elliptical' | 'other' (cardio) */
sport?: string
/** Distance parcourue (cardio), en km. */
distanceKm?: number
/** Intensité ressentie 1-5 (RPE simplifié). */
intensityRating?: 1 | 2 | 3 | 4 | 5
/** Groupes musculaires travaillés (strength_free). */
quickLogMuscleGroups?: string[]
```

- [ ] **Step 2 : Créer `src/utils/quickLog.ts` avec les types et la fonction pure**

```ts
// Logique pure du wizard Quick Log — sans dépendance IDB ni React.

export type ActivityKind = 'cardio' | 'team_sport' | 'strength_free' | 'other'

export interface CardioParams {
  kind: 'cardio'
  sport: 'running' | 'cycling' | 'swimming' | 'rowing' | 'elliptical' | 'other'
  durationMin: number
  distanceKm?: number
  intensityRating: 1 | 2 | 3 | 4 | 5
  notes?: string
}

export interface TeamSportParams {
  kind: 'team_sport'
  sport: string
  durationMin: number
  intensityRating: 1 | 2 | 3 | 4 | 5
  notes?: string
}

export interface StrengthFreeParams {
  kind: 'strength_free'
  durationMin: number
  muscleGroups: string[]
  intensityRating: 1 | 2 | 3 | 4 | 5
  notes?: string
}

export interface OtherParams {
  kind: 'other'
  label: string
  durationMin: number
  intensityRating: 1 | 2 | 3 | 4 | 5
  notes?: string
}

export type ActivityParams =
  | CardioParams
  | TeamSportParams
  | StrengthFreeParams
  | OtherParams

/** Labels d'affichage pour chaque activité cardio. */
export const CARDIO_SPORTS: { value: CardioParams['sport']; label: string; emoji: string }[] = [
  { value: 'running',    label: 'Course à pied', emoji: '🏃' },
  { value: 'cycling',    label: 'Vélo',           emoji: '🚴' },
  { value: 'swimming',   label: 'Natation',       emoji: '🏊' },
  { value: 'rowing',     label: 'Rameur',         emoji: '🚣' },
  { value: 'elliptical', label: 'Elliptique',     emoji: '⚙️' },
  { value: 'other',      label: 'Autre cardio',   emoji: '❤️' },
]

/** Déduit le nom de la séance à afficher dans l'historique. */
export function sessionNameFromParams(params: ActivityParams): string {
  switch (params.kind) {
    case 'cardio': {
      const found = CARDIO_SPORTS.find((s) => s.value === params.sport)
      return found ? found.label : 'Cardio'
    }
    case 'team_sport':
      return params.sport || 'Sport collectif'
    case 'strength_free':
      return 'Musculation libre'
    case 'other':
      return params.label || 'Activité'
  }
}

/** Emoji représentatif de l'activité (utilisé dans l'historique). */
export function emojiFromParams(params: ActivityParams): string {
  switch (params.kind) {
    case 'cardio': {
      const found = CARDIO_SPORTS.find((s) => s.value === params.sport)
      return found?.emoji ?? '🏃'
    }
    case 'team_sport': return '⚽'
    case 'strength_free': return '🏋️'
    case 'other': return '🏃'
  }
}
```

- [ ] **Step 3 : Créer `tests/quickLog.test.ts` et lancer les tests**

```ts
import { describe, it, expect } from 'vitest'
import {
  sessionNameFromParams,
  emojiFromParams,
  type CardioParams,
  type TeamSportParams,
  type StrengthFreeParams,
  type OtherParams,
} from '../src/utils/quickLog'

describe('sessionNameFromParams', () => {
  it('cardio running → "Course à pied"', () => {
    const p: CardioParams = { kind: 'cardio', sport: 'running', durationMin: 40, intensityRating: 3 }
    expect(sessionNameFromParams(p)).toBe('Course à pied')
  })
  it('cardio other → "Cardio"', () => {
    const p: CardioParams = { kind: 'cardio', sport: 'other', durationMin: 30, intensityRating: 2 }
    expect(sessionNameFromParams(p)).toBe('Cardio')
  })
  it('team_sport football → "Football"', () => {
    const p: TeamSportParams = { kind: 'team_sport', sport: 'Football', durationMin: 90, intensityRating: 4 }
    expect(sessionNameFromParams(p)).toBe('Football')
  })
  it('team_sport vide → "Sport collectif"', () => {
    const p: TeamSportParams = { kind: 'team_sport', sport: '', durationMin: 60, intensityRating: 3 }
    expect(sessionNameFromParams(p)).toBe('Sport collectif')
  })
  it('strength_free → "Musculation libre"', () => {
    const p: StrengthFreeParams = { kind: 'strength_free', durationMin: 45, muscleGroups: ['chest'], intensityRating: 3 }
    expect(sessionNameFromParams(p)).toBe('Musculation libre')
  })
  it('other avec label → label', () => {
    const p: OtherParams = { kind: 'other', label: 'Yoga', durationMin: 30, intensityRating: 2 }
    expect(sessionNameFromParams(p)).toBe('Yoga')
  })
  it('other sans label → "Activité"', () => {
    const p: OtherParams = { kind: 'other', label: '', durationMin: 20, intensityRating: 1 }
    expect(sessionNameFromParams(p)).toBe('Activité')
  })
})

describe('emojiFromParams', () => {
  it('cycling → 🚴', () => {
    const p: CardioParams = { kind: 'cardio', sport: 'cycling', durationMin: 60, intensityRating: 3 }
    expect(emojiFromParams(p)).toBe('🚴')
  })
  it('team_sport → ⚽', () => {
    const p: TeamSportParams = { kind: 'team_sport', sport: 'Tennis', durationMin: 60, intensityRating: 3 }
    expect(emojiFromParams(p)).toBe('⚽')
  })
  it('strength_free → 🏋️', () => {
    const p: StrengthFreeParams = { kind: 'strength_free', durationMin: 45, muscleGroups: [], intensityRating: 3 }
    expect(emojiFromParams(p)).toBe('🏋️')
  })
})
```

Lancer : `npm test -- tests/quickLog.test.ts`
Attendu : 10/10 ✅

- [ ] **Step 4 : Commit**

```bash
git add src/types.ts src/utils/quickLog.ts tests/quickLog.test.ts
git commit -m "feat: add SessionKind + ActivityParams types and pure quickLog utils"
```

---

### Task 2 — Fonction async `logActivitySession` dans `sessionOps.ts`

**Files:**
- Modify: `src/utils/sessionOps.ts`
- Modify: `tests/quickLog.test.ts` (ajout de tests async)

**Interfaces:**
- Consomme: `ActivityParams` de `src/utils/quickLog.ts`
- Consomme: `StoreApi` de `src/hooks/useStore`
- Produit: `logActivitySession(params, store): Promise<Session>` utilisé par Task 3.

---

- [ ] **Step 1 : Ajouter les tests async dans `tests/quickLog.test.ts`**

Ajouter à la fin du fichier :

```ts
import { logActivitySession } from '../src/utils/sessionOps'
import type { StoreApi } from '../src/hooks/useStore'

function makeStore(overrides: Partial<StoreApi> = {}): StoreApi {
  return {
    sessions: [],
    session: { save: vi.fn(async (s) => s), remove: vi.fn() },
    exercises: [],
    sessionExercises: [],
    sessionExercise: { save: vi.fn(async (s) => s), remove: vi.fn() },
    sets: [],
    set: { save: vi.fn(async (s) => s), remove: vi.fn() },
    programs: [],
    program: { save: vi.fn(async (s) => s), remove: vi.fn() },
    workoutTemplates: [],
    workoutTemplate: { save: vi.fn(async (s) => s), remove: vi.fn() },
    workoutExerciseTemplates: [],
    workoutExerciseTemplate: { save: vi.fn(async (s) => s), remove: vi.fn() },
    personalRecords: [],
    personalRecord: { save: vi.fn(async (s) => s), remove: vi.fn() },
    goals: [],
    goal: { save: vi.fn(async (s) => s), remove: vi.fn() },
    bodyMeasurements: [],
    bodyMeasurement: { save: vi.fn(async (s) => s), remove: vi.fn() },
    blobs: [],
    blob: { save: vi.fn(async (s) => s), remove: vi.fn() },
    settings: {
      id: 'singleton', firstName: '', lastName: '', createdAt: 0,
      updatedAt: 0, deleted: false, dirty: false,
      preferences: {
        weightUnit: 'kg', distanceUnit: 'km', measurementUnit: 'cm',
        defaultRestSec: 90, weightStep: 2.5, restSoundEnabled: true,
        hapticsEnabled: true, autoBarbellWeight: true, autoWarmup: false,
        theme: 'auto', accentColor: '#5b9dff', language: 'fr',
        weekStart: 'monday', rpeScale: '6-10', oneRMFormula: 'epley',
        notificationsEnabled: false, skipDayPreview: false,
        skipBriefing: false, prCelebrationEnabled: true,
      },
    },
    ...overrides,
  } as unknown as StoreApi
}

describe('logActivitySession', () => {
  it('crée une session cardio avec durationSec + sessionKind', async () => {
    const store = makeStore()
    const params: CardioParams = {
      kind: 'cardio', sport: 'running', durationMin: 40, intensityRating: 3,
    }
    await logActivitySession(params, store)
    const saved = (store.session.save as ReturnType<typeof vi.fn>).mock.calls[0]?.[0]
    expect(saved.name).toBe('Course à pied')
    expect(saved.sessionKind).toBe('cardio')
    expect(saved.sport).toBe('running')
    expect(saved.durationSec).toBe(2400)
    expect(saved.intensityRating).toBe(3)
    expect(saved.endedAt).toBeTypeOf('number')
    expect(saved.totalSets).toBe(0)
  })

  it('crée une session strength_free avec muscleGroups', async () => {
    const store = makeStore()
    const params: StrengthFreeParams = {
      kind: 'strength_free', durationMin: 45,
      muscleGroups: ['chest', 'triceps'], intensityRating: 4,
    }
    await logActivitySession(params, store)
    const saved = (store.session.save as ReturnType<typeof vi.fn>).mock.calls[0]?.[0]
    expect(saved.sessionKind).toBe('strength_free')
    expect(saved.quickLogMuscleGroups).toEqual(['chest', 'triceps'])
    expect(saved.durationSec).toBe(2700)
  })

  it('crée une session team_sport avec distanceKm absent', async () => {
    const store = makeStore()
    const params: TeamSportParams = {
      kind: 'team_sport', sport: 'Football', durationMin: 90, intensityRating: 5,
    }
    await logActivitySession(params, store)
    const saved = (store.session.save as ReturnType<typeof vi.fn>).mock.calls[0]?.[0]
    expect(saved.sessionKind).toBe('team_sport')
    expect(saved.distanceKm).toBeUndefined()
  })

  it('inclut distanceKm si fourni', async () => {
    const store = makeStore()
    const params: CardioParams = {
      kind: 'cardio', sport: 'cycling', durationMin: 60,
      distanceKm: 25, intensityRating: 3,
    }
    await logActivitySession(params, store)
    const saved = (store.session.save as ReturnType<typeof vi.fn>).mock.calls[0]?.[0]
    expect(saved.distanceKm).toBe(25)
  })
})
```

- [ ] **Step 2 : Vérifier que les tests échouent**

Lancer : `npm test -- tests/quickLog.test.ts`
Attendu : `logActivitySession is not a function` ou import error.

- [ ] **Step 3 : Implémenter `logActivitySession` dans `src/utils/sessionOps.ts`**

Ajouter à la fin du fichier, après `startFreestyleSession` :

```ts
import type { ActivityParams } from './quickLog'
import { sessionNameFromParams } from './quickLog'

/**
 * Enregistre immédiatement une activité terminée (cardio, sport collectif,
 * musculation libre, autre) sans ouvrir la modale de séance active.
 */
export async function logActivitySession(
  params: ActivityParams,
  store: StoreApi,
): Promise<Session> {
  const now = Date.now()
  const durationSec = params.durationMin * 60

  return store.session.save({
    id: uuid(),
    name: sessionNameFromParams(params),
    sessionKind: params.kind,
    sport: params.kind === 'cardio' ? params.sport
         : params.kind === 'team_sport' ? params.sport
         : undefined,
    distanceKm: params.kind === 'cardio' ? params.distanceKm : undefined,
    intensityRating: params.intensityRating,
    quickLogMuscleGroups: params.kind === 'strength_free' ? params.muscleGroups : undefined,
    notes: params.notes,
    startedAt: now - durationSec * 1000,
    endedAt: now,
    durationSec,
    totalSets: 0,
    completedSets: 0,
  })
}
```

- [ ] **Step 4 : Vérifier que les tests passent**

Lancer : `npm test -- tests/quickLog.test.ts`
Attendu : 14/14 ✅

- [ ] **Step 5 : Typecheck**

```bash
npm run typecheck
```
Attendu : 0 erreur.

- [ ] **Step 6 : Commit**

```bash
git add src/utils/quickLog.ts src/utils/sessionOps.ts tests/quickLog.test.ts
git commit -m "feat: add logActivitySession — creates a completed quick-log session"
```

---

### Task 3 — Écran `QuickLogScreen` (wizard multi-étapes)

**Files:**
- Create: `src/components/screens/QuickLogScreen.tsx`

**Interfaces:**
- Consomme: `ActivityParams`, `CARDIO_SPORTS` de `src/utils/quickLog.ts`
- Consomme: `logActivitySession` de `src/utils/sessionOps.ts`
- Consomme: `useStore`, `useNavigation`

---

- [ ] **Step 1 : Créer `src/components/screens/QuickLogScreen.tsx`**

```tsx
import { useState } from 'react'
import { useStore } from '../../hooks/useStore'
import { useNavigation } from '../../nav/useNavigation'
import { logActivitySession, CARDIO_SPORTS } from '../../utils/quickLog'
import type { ActivityParams, CardioParams, TeamSportParams, StrengthFreeParams, OtherParams } from '../../utils/quickLog'
import { Button, Icon } from '../ui'

// ── Composant principal ───────────────────────────────────────────────────────

type Step = 'kind' | 'details' | 'confirm'

const MUSCLE_GROUP_OPTIONS = [
  { value: 'chest',      label: 'Pectoraux' },
  { value: 'back',       label: 'Dos' },
  { value: 'shoulders',  label: 'Épaules' },
  { value: 'biceps',     label: 'Biceps' },
  { value: 'triceps',    label: 'Triceps' },
  { value: 'quads',      label: 'Quadriceps' },
  { value: 'hamstrings', label: 'Ischios' },
  { value: 'glutes',     label: 'Fessiers' },
  { value: 'core',       label: 'Abdos' },
  { value: 'calves',     label: 'Mollets' },
]

const INTENSITY_LABELS: Record<number, string> = {
  1: 'Légère',
  2: 'Modérée',
  3: 'Moyenne',
  4: 'Intense',
  5: 'Maximale',
}

export function QuickLogScreen() {
  const store = useStore()
  const nav = useNavigation()
  const [step, setStep] = useState<Step>('kind')
  const [kind, setKind] = useState<ActivityParams['kind'] | null>(null)
  const [durationMin, setDurationMin] = useState(45)
  const [intensity, setIntensity] = useState<1 | 2 | 3 | 4 | 5>(3)
  const [cardioSport, setCardioSport] = useState<CardioParams['sport']>('running')
  const [distanceKm, setDistanceKm] = useState<string>('')
  const [teamSport, setTeamSport] = useState('Football')
  const [selectedMuscles, setSelectedMuscles] = useState<string[]>([])
  const [otherLabel, setOtherLabel] = useState('')
  const [notes, setNotes] = useState('')
  const [saving, setSaving] = useState(false)

  const toggleMuscle = (val: string) => {
    setSelectedMuscles((prev) =>
      prev.includes(val) ? prev.filter((m) => m !== val) : [...prev, val],
    )
  }

  const buildParams = (): ActivityParams | null => {
    if (!kind) return null
    switch (kind) {
      case 'cardio':
        return {
          kind: 'cardio',
          sport: cardioSport,
          durationMin,
          distanceKm: distanceKm ? parseFloat(distanceKm) : undefined,
          intensityRating: intensity,
          notes: notes || undefined,
        }
      case 'team_sport':
        return {
          kind: 'team_sport',
          sport: teamSport,
          durationMin,
          intensityRating: intensity,
          notes: notes || undefined,
        }
      case 'strength_free':
        return {
          kind: 'strength_free',
          durationMin,
          muscleGroups: selectedMuscles,
          intensityRating: intensity,
          notes: notes || undefined,
        }
      case 'other':
        return {
          kind: 'other',
          label: otherLabel,
          durationMin,
          intensityRating: intensity,
          notes: notes || undefined,
        }
    }
  }

  const handleSave = async () => {
    const params = buildParams()
    if (!params) return
    setSaving(true)
    try {
      await logActivitySession(params, store)
      nav.goBack()
    } finally {
      setSaving(false)
    }
  }

  // ── Écran 1 : choix du type ──────────────────────────────────────────────

  if (step === 'kind') {
    const KINDS: { value: ActivityParams['kind']; emoji: string; label: string; sub: string }[] = [
      { value: 'cardio',        emoji: '🏃', label: 'Cardio',          sub: 'Course, vélo, natation…' },
      { value: 'strength_free', emoji: '🏋️', label: 'Musculation',     sub: 'Full body, haut du corps…' },
      { value: 'team_sport',    emoji: '⚽', label: 'Sport collectif', sub: 'Football, basket, tennis…' },
      { value: 'other',         emoji: '🧘', label: 'Autre',           sub: 'Yoga, escalade, danse…' },
    ]
    return (
      <div className="gt-screen">
        <div className="gt-topbar">
          <button className="gt-icon-btn" onClick={() => nav.goBack()} aria-label="Retour">
            <Icon name="chevron-left" size={22} />
          </button>
          <h1 className="gt-topbar__title">Enregistrer une séance</h1>
        </div>
        <div className="gt-screen__scroll" style={{ padding: 'var(--gap-section)' }}>
          <p className="t-body" style={{ color: 'var(--fg-muted)', marginBottom: 'var(--gap-section)' }}>
            Quel type d'activité ?
          </p>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--gap-card)' }}>
            {KINDS.map((k) => (
              <button
                key={k.value}
                onClick={() => { setKind(k.value); setStep('details') }}
                style={{
                  display: 'flex', alignItems: 'center', gap: 16,
                  background: 'var(--surface)', border: '1.5px solid var(--border)',
                  borderRadius: 'var(--radius-card)', padding: '16px 18px',
                  cursor: 'pointer', textAlign: 'left', width: '100%',
                }}
              >
                <span style={{ fontSize: 32 }}>{k.emoji}</span>
                <div>
                  <div className="t-body" style={{ fontWeight: 600 }}>{k.label}</div>
                  <div className="t-caption" style={{ color: 'var(--fg-muted)' }}>{k.sub}</div>
                </div>
                <Icon name="chevron-right" size={18} style={{ marginLeft: 'auto', color: 'var(--fg-muted)' }} />
              </button>
            ))}
          </div>
        </div>
      </div>
    )
  }

  // ── Écran 2 : détails (selon le type) ────────────────────────────────────

  return (
    <div className="gt-screen">
      <div className="gt-topbar">
        <button className="gt-icon-btn" onClick={() => setStep('kind')} aria-label="Retour">
          <Icon name="chevron-left" size={22} />
        </button>
        <h1 className="gt-topbar__title">Détails</h1>
      </div>
      <div className="gt-screen__scroll" style={{ padding: 'var(--gap-section)', display: 'flex', flexDirection: 'column', gap: 'var(--gap-section)' }}>

        {/* ── Cardio ── */}
        {kind === 'cardio' && (
          <section>
            <div className="t-eyebrow" style={{ marginBottom: 'var(--gap-tile)' }}>Activité</div>
            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 'var(--gap-tile)' }}>
              {CARDIO_SPORTS.map((s) => (
                <button
                  key={s.value}
                  onClick={() => setCardioSport(s.value)}
                  style={{
                    background: cardioSport === s.value ? 'var(--accent)' : 'var(--surface)',
                    color: cardioSport === s.value ? '#fff' : 'var(--fg)',
                    border: 'none', borderRadius: 'var(--radius-card)',
                    padding: '10px 8px', cursor: 'pointer', display: 'flex',
                    alignItems: 'center', gap: 8, fontSize: 'var(--fs-caption)',
                  }}
                >
                  <span>{s.emoji}</span> {s.label}
                </button>
              ))}
            </div>
          </section>
        )}

        {/* ── Sport collectif ── */}
        {kind === 'team_sport' && (
          <section>
            <div className="t-eyebrow" style={{ marginBottom: 'var(--gap-tile)' }}>Sport</div>
            <input
              className="gt-input"
              value={teamSport}
              onChange={(e) => setTeamSport(e.target.value)}
              placeholder="Football, Basketball, Tennis…"
            />
          </section>
        )}

        {/* ── Autre ── */}
        {kind === 'other' && (
          <section>
            <div className="t-eyebrow" style={{ marginBottom: 'var(--gap-tile)' }}>Activité</div>
            <input
              className="gt-input"
              value={otherLabel}
              onChange={(e) => setOtherLabel(e.target.value)}
              placeholder="Yoga, Escalade, Danse…"
            />
          </section>
        )}

        {/* ── Groupes musculaires (strength_free) ── */}
        {kind === 'strength_free' && (
          <section>
            <div className="t-eyebrow" style={{ marginBottom: 'var(--gap-tile)' }}>Zones travaillées (optionnel)</div>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: 'var(--gap-tile)' }}>
              {MUSCLE_GROUP_OPTIONS.map((m) => (
                <button
                  key={m.value}
                  onClick={() => toggleMuscle(m.value)}
                  style={{
                    background: selectedMuscles.includes(m.value) ? 'var(--accent)' : 'var(--surface)',
                    color: selectedMuscles.includes(m.value) ? '#fff' : 'var(--fg)',
                    border: 'none', borderRadius: 100, padding: '6px 14px',
                    cursor: 'pointer', fontSize: 'var(--fs-caption)',
                  }}
                >
                  {m.label}
                </button>
              ))}
            </div>
          </section>
        )}

        {/* ── Durée ── */}
        <section>
          <div className="t-eyebrow" style={{ marginBottom: 'var(--gap-tile)' }}>
            Durée — <span className="t-num">{durationMin}</span> min
          </div>
          <input
            type="range" min={5} max={180} step={5}
            value={durationMin}
            onChange={(e) => setDurationMin(Number(e.target.value))}
            style={{ width: '100%', accentColor: 'var(--accent)' }}
          />
          <div style={{ display: 'flex', justifyContent: 'space-between' }}>
            <span className="t-caption" style={{ color: 'var(--fg-muted)' }}>5 min</span>
            <span className="t-caption" style={{ color: 'var(--fg-muted)' }}>3 h</span>
          </div>
        </section>

        {/* ── Distance (cardio seulement) ── */}
        {kind === 'cardio' && (
          <section>
            <div className="t-eyebrow" style={{ marginBottom: 'var(--gap-tile)' }}>Distance (optionnel)</div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
              <input
                className="gt-input"
                type="number" inputMode="decimal" min={0} step={0.1}
                value={distanceKm}
                onChange={(e) => setDistanceKm(e.target.value)}
                placeholder="0.0"
                style={{ flex: 1 }}
              />
              <span className="t-body">{store.settings.preferences.distanceUnit}</span>
            </div>
          </section>
        )}

        {/* ── Intensité ── */}
        <section>
          <div className="t-eyebrow" style={{ marginBottom: 'var(--gap-tile)' }}>
            Intensité — <span style={{ color: 'var(--accent)' }}>{INTENSITY_LABELS[intensity]}</span>
          </div>
          <div style={{ display: 'flex', gap: 'var(--gap-tile)' }}>
            {([1, 2, 3, 4, 5] as const).map((v) => (
              <button
                key={v}
                onClick={() => setIntensity(v)}
                style={{
                  flex: 1, padding: '10px 0',
                  background: intensity === v ? 'var(--accent)' : 'var(--surface)',
                  color: intensity === v ? '#fff' : 'var(--fg)',
                  border: 'none', borderRadius: 'var(--radius-card)',
                  fontWeight: 700, cursor: 'pointer',
                  fontSize: 'var(--fs-body)',
                }}
              >
                {v}
              </button>
            ))}
          </div>
        </section>

        {/* ── Notes (optionnel) ── */}
        <section>
          <div className="t-eyebrow" style={{ marginBottom: 'var(--gap-tile)' }}>Note (optionnel)</div>
          <textarea
            className="gt-input"
            value={notes}
            onChange={(e) => setNotes(e.target.value)}
            placeholder="Comment s'est passée la séance ?"
            rows={2}
            style={{ resize: 'none', width: '100%' }}
          />
        </section>

        <Button
          variant="primary"
          disabled={saving}
          onClick={handleSave}
          style={{ marginTop: 8 }}
        >
          {saving ? 'Enregistrement…' : 'Enregistrer la séance'}
        </Button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2 : Build check**

```bash
npm run build
```
Attendu : 0 erreur TypeScript, bundle valide.

- [ ] **Step 3 : Commit**

```bash
git add src/components/screens/QuickLogScreen.tsx
git commit -m "feat: add QuickLogScreen wizard (4 activity types)"
```

---

### Task 4 — Navigation + point d'entrée Dashboard

**Files:**
- Modify: `src/nav/navigation.ts` (ajouter `'quickLog'` à `ScreenName`)
- Modify: `src/nav/screenRegistry.tsx` (enregistrer `QuickLogScreen`)
- Modify: `src/components/screens/DashboardScreen.tsx` (bouton d'entrée)

---

- [ ] **Step 1 : Ajouter `'quickLog'` à `ScreenName` dans `src/nav/navigation.ts`**

Remplacer :
```ts
  | 'admin'
```
par :
```ts
  | 'admin'
  | 'quickLog'
```

- [ ] **Step 2 : Enregistrer l'écran dans `src/nav/screenRegistry.tsx`**

Ajouter l'import :
```ts
import { QuickLogScreen } from '../components/screens/QuickLogScreen'
```

Dans `SCREENS`, ajouter :
```ts
  quickLog: QuickLogScreen,
```

- [ ] **Step 3 : Ajouter le bouton dans `src/components/screens/DashboardScreen.tsx`**

Dans la section du scroll du Dashboard (chercher `gt-screen__scroll`), ajouter juste après la section "Reprendre séance en cours" (après le bloc `resumable &&`) :

```tsx
{/* ── Enregistrer une séance rapide ─────────────────────────────── */}
<Card onClick={() => nav.push('quickLog')}>
  <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
    <div style={{ color: 'var(--accent)', display: 'flex' }}>
      <Icon name="plus" size={24} />
    </div>
    <div style={{ textAlign: 'left' }}>
      <div style={{ fontWeight: 600 }}>Enregistrer une séance</div>
      <div style={{ fontSize: 12, color: 'var(--fg-muted)' }}>
        Course, sport, full body…
      </div>
    </div>
    <Icon name="chevron-right" size={18} style={{ marginLeft: 'auto', color: 'var(--fg-muted)' }} />
  </div>
</Card>
```

- [ ] **Step 4 : Build check + typecheck**

```bash
npm run build && npm run typecheck
```
Attendu : 0 erreur.

- [ ] **Step 5 : Commit**

```bash
git add src/nav/navigation.ts src/nav/screenRegistry.tsx src/components/screens/DashboardScreen.tsx
git commit -m "feat: wire QuickLogScreen into navigation and dashboard"
```

---

### Task 5 — Affichage dans l'historique

**Files:**
- Modify: `src/components/screens/HistoryScreen.tsx`

---

- [ ] **Step 1 : Ajouter un emoji de kind dans la ligne de session**

Dans `HistoryScreen.tsx`, repérer l'endroit où les sessions sont affichées dans la liste (chercher `s.name`). Avant le nom, afficher l'emoji :

```tsx
// Ajouter cette fonction en haut du fichier (après les imports)
function kindEmoji(s: Session): string {
  if (s.sessionKind === 'cardio') {
    if (s.sport === 'running')    return '🏃'
    if (s.sport === 'cycling')    return '🚴'
    if (s.sport === 'swimming')   return '🏊'
    if (s.sport === 'rowing')     return '🚣'
    if (s.sport === 'elliptical') return '⚙️'
    return '❤️'
  }
  if (s.sessionKind === 'team_sport')    return '⚽'
  if (s.sessionKind === 'strength_free') return '🏋️'
  if (s.sessionKind === 'other')         return '🧘'
  return '💪'
}
```

Puis, dans le rendu de chaque session, préfixer le nom par `kindEmoji(s) + ' '`.

L'import `Session` est déjà disponible via `useStore` — pas besoin de l'importer séparément.

- [ ] **Step 2 : Build + typecheck**

```bash
npm run build && npm run typecheck
```
Attendu : 0 erreur.

- [ ] **Step 3 : Lancer tous les tests**

```bash
npm test
```
Attendu : tous les tests existants + les 14 nouveaux quickLog passent.

- [ ] **Step 4 : Commit final + push**

```bash
git add src/components/screens/HistoryScreen.tsx
git commit -m "feat: show activity kind emoji in history list"
git push origin main
```

---

## Récapitulatif des fichiers touchés

| Fichier | Action |
|---------|--------|
| `src/types.ts` | +5 champs optionnels sur `Session` |
| `src/utils/quickLog.ts` | Nouveau — types + fonctions pures |
| `src/utils/sessionOps.ts` | +`logActivitySession()` |
| `src/nav/navigation.ts` | +`'quickLog'` dans `ScreenName` |
| `src/nav/screenRegistry.tsx` | +import + enregistrement |
| `src/components/screens/QuickLogScreen.tsx` | Nouveau — wizard 2 écrans |
| `src/components/screens/DashboardScreen.tsx` | +bouton d'entrée |
| `src/components/screens/HistoryScreen.tsx` | +emoji kind |
| `tests/quickLog.test.ts` | Nouveau — 14 tests |
